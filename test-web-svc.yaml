---
apiVersion: v1
kind: Service
metadata:
  name: test-web
  namespace: test
#  annotations:
#    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  selector:
    name: test-web
  ports:
  - port: 7180
    targetPort: 7180
    nodePort: 30280
  type: NodePort
